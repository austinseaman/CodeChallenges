import React from 'react'

const Square = (props) => {
    const styles = {
        backgroundColor: props.color,
        border: "1px solid black"
    }
    return (
        <div style={styles}>

        </div>
    )
}

export default Square